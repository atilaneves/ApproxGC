Import('testEnvironment')

ut_main = testEnvironment['LIBRARIES']['unit_threaded'].UnitThreadedMakeMain('ut_main.d')

unitTests = testEnvironment.Program('unit-test', ['main.d', ut_main], DFLAGS=['-unittest'])

Return('unitTests')
